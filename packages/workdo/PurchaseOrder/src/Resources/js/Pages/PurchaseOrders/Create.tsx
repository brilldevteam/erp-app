import { Head,useForm } from '@inertiajs/react'; import AuthenticatedLayout from '@/layouts/authenticated-layout'; import { Card,CardContent } from '@/components/ui/card'; import Form,{emptyLine} from './Form';
export default function Create(props:any){const f=useForm({vendor_id:'',warehouse_id:'',order_date:new Date().toISOString().slice(0,10),expected_delivery_date:'',vendor_reference:'',vendor_quotation_number:'',document_discount_type:'fixed',document_discount_value:0,shipping_amount:0,adjustment_amount:0,notes:'',terms:'',items:[{...emptyLine}],attachments:[] as File[]});return <AuthenticatedLayout breadcrumbs={[{label:'Purchase Orders',url:route('purchase-orders.index')},{label:'Create'}]} pageTitle="Create Purchase Order / LPO"><Head title="Create Purchase Order"/><Card><CardContent className="pt-6"><Form {...props} {...f} onSubmit={(e:any)=>{e.preventDefault();f.post(route('purchase-orders.store'),{forceFormData:true})}} submitLabel="Create Purchase Order"/></CardContent></Card></AuthenticatedLayout>}
